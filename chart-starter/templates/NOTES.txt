Application {{ include "app.fullname" . }} deployed successfully!

Namespace: {{ .Values.namespace }}
Release: {{ .Release.Name }}

{{- if .Values.ingress.enabled }}

Access via:
{{- if eq .Values.ingress.type "public" }}
  {{if .Values.ingress.public.tls.enabled }}https{{ else }}http{{ end }}://{{ .Values.ingress.host }}
{{- else if eq .Values.ingress.type "private" }}
  {{- if .Values.ingress.private.tls.enabled }}https{{ else }}http{{ end }}://{{ .Values.ingress.host }}
{{- end }}

{{- else }}

To access the application:

  kubectl port-forward -n {{ .Values.namespace }} svc/{{ include "app.fullname" . }} 8080:{{ .Values.service.port }}

Then visit: http://localhost:8080

{{- end }}

{{- if .Values.metrics.enabled }}

Metrics available at:
  http://{{ include "app.fullname" . }}.{{ .Values.namespace }}.svc:{{ .Values.metrics.port }}{{ .Values.metrics.path }}

{{- end }}

{{- if eq .Values.secrets.type "sops" }}

NOTE: Make sure SOPS secrets are encrypted before committing to git.
{{- end }}

{{- if eq .Values.storage.type "nfs" }}

NOTE: Using NFS storage. Make sure NFS server is accessible and paths exist.
{{- end }}
