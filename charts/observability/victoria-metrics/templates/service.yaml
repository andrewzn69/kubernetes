apiVersion: v1
kind: Service
metadata:
  name: {{ include "victoria-metrics.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ include "victoria-metrics.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "victoria-metrics.fullname" . }}
