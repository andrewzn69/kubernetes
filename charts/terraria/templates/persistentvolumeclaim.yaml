apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: terraria-world-data
  namespace: {{ .Values.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  volumeName: terraria-world-data
  resources:
    requests:
      storage: {{ .Values.persistence.worldData.storage }}
