apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: zemn-wildcard
  namespace: default
spec:
  secretName: zemn-wildcard-tls
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
  dnsNames:
  - "zemn.xyz"
  - "*.zemn.xyz"
