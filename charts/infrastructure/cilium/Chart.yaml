apiVersion: v2
name: cilium
description: Cilium CNI with Gateway API support
type: application
version: "1.0.0"

dependencies:
  - name: cilium
    version: 1.19.4
    repository: https://helm.cilium.io/
