# longhorn - distributed block storage (csi driver)

# privilege escalation - csi driver needs elevated permissions
KSV-0001 charts/infrastructure/longhorn

# runs as root - csi driver components need root for volume operations
KSV-0012 charts/infrastructure/longhorn

# readOnlyRootFilesystem - csi components need writable fs
KSV-0014 charts/infrastructure/longhorn

# privileged containers - required for volume management
KSV-0017 charts/infrastructure/longhorn

# hostpath volumes - csi driver needs host fs access
KSV-0023 charts/infrastructure/longhorn

# delete pod logs - upstream chart permission (really though?)
KSV-0042 charts/infrastructure/longhorn

# manage secrets - backup credentials, encryption keys
KSV-0041 charts/infrastructure/longhorn

# wildcard verbs - dynamic volume provisioning
KSV-0045 charts/infrastructure/longhorn

# manage workloads - volume attachment, pod scheduling
KSV-0048 charts/infrastructure/longhorn

# manage configmaps - driver config, settings
KSV-0049 charts/infrastructure/longhorn

# rbac escalation - create volume-specific service accounts
KSV-0050 charts/infrastructure/longhorn

# networking permissions - volume endpoints, services
KSV-0056 charts/infrastructure/longhorn

# seccomp profiles - upstream chart does not set runtimedefault
KSV-0104 charts/infrastructure/longhorn

# configmap secrets - backup target credential references
KSV-0109 charts/infrastructure/longhorn

# admin role binding - support bundle needs cluster-wide read access for diagnostics
KSV-0111 charts/infrastructure/longhorn

# manage ns secrets - encryption keys, backup creds
KSV-0113 charts/infrastructure/longhorn

# manage webhooks - admission control
KSV-0114 charts/infrastructure/longhorn

# default security context - longhorn-manager needs root
KSV-0118 charts/infrastructure/longhorn
